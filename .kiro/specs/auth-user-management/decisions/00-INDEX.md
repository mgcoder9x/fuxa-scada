# 00 — INDEX & Integrity Manifest (read this FIRST)

> **Why this file exists.** On 2026-07-13, `03-tradeoffs.md` was found overwritten with an
> unrelated chat transcript (N-020) — decision provenance was destroyed. This manifest is the
> **integrity layer** of the anti-drift kit: it declares what every spec file MUST contain, so a
> future agent (or the audit hook) can detect corruption, truncation, or silent re-decision.
>
> **Session start-up order:** (1) read this manifest, (2) run the Integrity Check below, (3) read
> the rest of `decisions/` in full, (4) run the anti-drift protocol in `README.md` §4, (5) obey
> the gates in `GATES.md`. Only then touch any spec artifact.

---

## 1. File manifest — declared purpose + required structural markers

> A file **fails integrity** if it does not contain its required markers, contains content from a
> different file's domain (e.g. a chat transcript, code unrelated to its role), or has lost its
> ID sequence. On failure: STOP, log a new `N-*` incident in `04-notes.md`, and rebuild from
> cross-references (never fabricate; quarantine unrecoverable IDs like TO-003).

| File | Purpose (one line) | Required markers (must all be present) |
|------|--------------------|----------------------------------------|
| `requirements.md` | 17 EARS requirements + glossary | `### Requirement 1`…`### Requirement 17`; `#### Acceptance Criteria`; EARS keywords (WHEN/IF/WHERE/WHILE/THE/SHALL) |
| `design.md` | Master map (arch + TOC + properties) | `## Architecture`; `## Table of Contents`; `## Correctness Properties`; `Property 1`…`Property 9` |
| `design/01`…`design/12` | Detailed design per area | each starts `# Design Section NN`; has `## Correctness Properties` (where it owns any) and a Traceability table |
| `decisions/README.md` | Ledger rules + ID scheme + anti-drift protocol | `## 2. Stable ID scheme`; `## 4. The anti-drift protocol` |
| `decisions/00-INDEX.md` | THIS manifest | `## 1. File manifest`; `## 3. Drift-incident log` |
| `decisions/01-ai-decisions.md` | `D-*` decisions | `### D-001`; monotonic `D-<nnn>` sequence; each entry has Rationale + Verification |
| `decisions/02-deviations.md` | `DV-*` deviations | `### DV-001`; monotonic `DV-<nnn>` sequence |
| `decisions/03-tradeoffs.md` | `TO-*` trade-offs | `### TO-001`; **must NOT contain chat transcripts or prose unrelated to trade-offs**; `Alternatives considered` per entry |
| `decisions/04-notes.md` | `N-*` facts/risks | `### N-001`; monotonic `N-<nnn>` sequence; unverified facts marked `UNVERIFIED` |
| `decisions/traceability.md` | REQ⇄DES⇄TASK⇄TEST matrix | `## B. Requirement → Design`; `## F. Design-Defect Register`; `## G.` (§D placeholder) |
| `decisions/GATES.md` | Phase gates + roadmap | `## Gates`; `## Roadmap` |
| `tasks.md` | Implementation plan | `## Tasks`; `## Task Dependency Graph` |
| `guide/*` | Vietnamese self-build guide (NON-normative) | each maps to a `tasks.md` task; **guide never overrides design** |

## 2. ID high-water marks (never reuse; increment only)

> Update these numbers whenever a new ID is added. A new ID below the high-water mark = drift.

| Prefix | Highest used | Notes |
|--------|--------------|-------|
| `REQ-` | 17 | |
| `D-`   | 045 | D-014…D-023 are **Active (CONFIRMED)** (2026-07-13); **D-024** (store-adapter own-connection reads per D-015; plain-INSERT role create per AC-9.5); **D-025** (2026-07-13) — reject malformed UTF-16 (lone surrogates) at the Password_Hasher seam + boundary validation, fixes the N-027 bcryptjs ~9.6s DoS; **D-026** (2026-07-14) — strip `__proto__` at the `deserialize` seam + object-spread compose in both store adapters, fixes the N-029 metadata round-trip loss / prototype-manipulation hazard; **D-027** (2026-07-14) — `tokenVersion` first-class end-to-end contract (default 0 + absent→0 coercion), fixes DEF-T1/DEF-T5; **D-028** (2026-07-14) — single `type` token-type claim (drop parallel `typ`), fixes DEF-T3; **D-029** (2026-07-14) — name `settings.auth.jwtIssuer`/`jwtAudience`, validate only when configured, fixes DEF-T4; **D-030** (2026-07-14) — Refresh_Token_Store SHA-256 at-rest + compare-and-swap single-use consume (implements D-019); **D-031** (2026-07-14) — DV-006 dummy-hash produced by the same Password_Hasher at the same cost, once, from a random secret (timing parity); **D-032** (2026-07-14) — the live-identity resolution is a PURE `Authorization_Service.resolveIdentity(claims, record)` (Task 8-testable without HTTP; the Task-13 middleware supplies the record), fixes the P-013 testability gap of §4.1's "middleware performs" wording. D-027..D-032 test-verified; **D-033** (2026-07-14) — atomic last-admin guard is the store-adapter method `User_Store.deleteGuarded(username, isAdministratorFn)` (one `BEGIN IMMEDIATE` txn; service injects the §05 predicate), fixes DEF-U1 + closes the N-016 TOCTOU / P-016; **D-034** (2026-07-14) — password-policy config `settings.auth.passwordMinLength` (default 12, code-points) + `passwordBlocklist` (case-insensitive, built-in default), enforced in User_Service before hashing (AC-4.6/4.7). D-033/D-034 test-verified; **D-035** (2026-07-14) — enrollment model: the CSPRNG one-time secret is delivered via an injected `EnrollmentChannel` seam (never logged/returned, D-022), with a `OneTimeEnrollmentTokenStore` (SHA-256 hashed-at-rest, TTL, single-use) as the automated channel; resolves the §3.2 ambiguity. D-035 test-verified; **D-036** (2026-07-15) — client verification strategy: pure framework-free `auth-protocol` core + thin `@Injectable` shells, verified headlessly via jest/ts-jest (`moduleNameMapper` runtime stubs) + isolated `tsc -p tsconfig.verify.json` type-check against Angular 18, NOT TestBed/karma (blocked by N-044); **D-037** (2026-07-15) — **PROPOSED, pending user confirmation** (touches FUXA-core `client/package.json` + reverts the dependabot bump, out of D-003) — remediate N-044 by restoring the Initial (`be8d1e7`) known-good client dep set (Angular-18-compatible) + preserving the Task-15 jest devDeps, then `ng build` to unblock the web-view + Tasks 16/17 + the D-014 cutover. **D-037 EXECUTED + CONFIRMED 2026-07-15 (N-047): `ng build` exit 0 + fresh `client/dist`, auth jest 22/22, web-view unblocked. No OPEN decision remains**; **D-038** (2026-07-15) — canonical client build/verify command is `ng build --configuration production` (matches FUXA's `Dockerfile`), NOT the bare `npm run build` (= JIT dev build → blank page, N-048); do not alter `angular.json` `defaultConfiguration`. Verified: production build reproduces the committed shipped `client/dist` byte-for-byte; **D-039** (2026-07-15) — the module's `Login_Page` is a **standalone** Angular component (Task 16.1 touches no FUXA-core file; route + `AuthGuard` cutover deferred to Task 17.4), the D-003-minimal registration for the verified all-NgModule/dialog-login FUXA app; **D-040** (2026-07-15) — added a top-level SPA `index.html` catch-all in `server/main.js` to fix the N-052 deep-route 404 (browser refresh/deep-link on ~13 client routes); a FUXA-CORE edit EXPLICITLY OUTSIDE the auth D-003 boundary, user-authorized; VERIFIED (13 routes 404→200, API/assets unaffected, `/alarms` renders via Playwright with 0 console errors); **D-041** (2026-07-15, enacts TO-015) — hardened Node-RED enabled-without-mode auth default from `legacy-open`→**`secure`** in `server/main.js` (fail-secure, matches the API path; N-054 footgun); keeps Node-RED runtime-toggled via the existing admin Settings; follow-ups = client `/flows` gating on live `nodeRedEnabled` + verify mount-on-restart. FUXA-core, outside auth D-003, user-authorized; **D-042** (2026-07-16) — **PROPOSED, pending user's approach choice** — Task 17.4 SUPERSEDE-cutover design (groups↔roles): validated integration facts (AuthGuard gates on groups-based `isAdmin()`; module login carries roles + doesn't update FUXA in-memory session, N-063); Option 1 full groups→roles rewrite (big/high-risk) vs **Option 2 (RECOMMENDED)** reuse FUXA's session mechanism from the module Login_Page + `legacy` flag (low-risk, reversible, no server cutover yet) vs Option 3 payload shim (rejected). Awaiting the user's Option 1-vs-2 choice before the reversible FUXA-core routing edit; **D-043** (2026-07-16) — Option-1 (full server SUPERSEDE + groups→roles) STAGED PLAN, design-validated against source: verified the cutover FORCE-ROTATES `admin`/`123456` (bootstrap DEF-B1 re-hash → enrollment) so admin/123456 stops working, and needs the N-060 enrollment-retrieval flow built first (operatorSink 0600 file + controlled-console + in-process single-use redeem endpoint). 4 stages (enrollment flow → flag-OFF server SUPERSEDE → client groups→roles → coordinated flip+e2e), reversible; PROPOSED/approval-gated (data-affecting); **D-044** (2026-07-16, enacts D-042 Option B / stage 3) — sign-in success payload PROJECTS RBAC onto FUXA's session shape: extend to `{token,username,fullname,roles,groups,info}` where `groups`=`-1` iff `Authorization_Service.isAdministrator(record)` else record passthrough, and `info`=`serialize({roles})` only (no metadata leak); root-correct because `UserService.create` never sets `groups` (module admins are role-based, so raw passthrough would misclassify them); reuses FUXA's tested client+server authz instead of rewriting it (avoids D-042 Option-1 blast radius); refines D-007's client-facing stance (roles stays the single authority, groups/info are derived views); authorization injected OPTIONAL into Authentication_Service (passthrough fallback keeps unit tests pure). Module-only/non-data-affecting; goes live at the stage-4 flip. Deployment note: non-admin widget perms need `settings.userRole=true`; **D-045** (2026-07-16, closes N-072) — forced first-login password-rotation UI: signin payload gains top-level `mustRotate` (actionable non-secret flag); `login.component` routes IN-APP to a new standalone `/auth/rotate-password` page (presenter+jest [DV-010] + `RotatePasswordClient` [D-036] + `normalizeRotateError`) when `mustRotate`; on success clears session→login to re-auth with the new secret. Server suite 170, client jest 74, `ng build` prod 0, browser e2e green [secret→login→auto-route→rotate→re-login→full admin, 0 console errors]. Module-only client + one additive server field; goes live at the stage-4 flip |
| `DV-`  | 012 | DV-006…DV-008 are **Active (CONFIRMED)** (2026-07-13) — requirement refinements enacted in `requirements.md`; **DV-009** (2026-07-15) — client HTTP-client tests use headless direct-instantiation instead of `HttpClientTestingModule` (verified blocker N-044; equivalent verification via the pure core + thin shells, D-036); **DV-010** (2026-07-15) — Login_Page (and later component) verified via a pure framework-free **presenter + headless jest**, NOT `TestBed`/karma as design/07 §9.1 says (VERIFIED: no `test` script, no `src/test.ts`/`karma.conf.js`, no karma/chrome/jasmine deps — the harness is absent); AC-11.1 template presence covered by production `ng build` + inspection; **DV-011** (2026-07-16) — edited a FUXA-core file (`home.component.ts`) with defensive `?.` null-guards (a D-003 in-place-edit deviation) to fix a verified crash on `secureEnabled=true`+empty-project (`this.hmi.layout.loginonstart`/`loginoverlaycolor`), browser-verified; **DV-012** (2026-07-17) — in-place FUXA-core edit of `auth.service.ts` `signIn()`: gate `console.error(err)` to unexpected statuses only (skip expected 401/429) so normal wrong-password/rate-limit outcomes don't pollute the commercial console / error-monitoring, while network/5xx still log; `observer.error` unchanged (UX identical); root-trace + browser-verified (N-077/N-078, `__cap===[]`); no OPEN deviation remains |
| `TO-`  | 016 | **TO-003 QUARANTINED (unrecoverable, N-020) — do not reuse**; **TO-012** (2026-07-14) — JWT key rotation: forward-compat `kid` + single active key now (Option A), multi-key keyring is a documented follow-up (DEF-T2); **TO-013** (2026-07-14) — cutover sequencing: client-first migration (Option 1, RECOMMENDED) vs transitional payload-compat shim vs parallel `/api/v2` — the FUXA-core `api/index.js` SUPERSEDE is hard-coupled to the client's groups→roles migration (N-042); **TO-014** (2026-07-15) — unblock the client build (N-044): restore the Initial known-good dep set (RECOMMENDED, root fix) vs migrate source to the bumped Angular-21/22 majors (infeasible on pinned Angular 18) vs bump Angular core (disproportionate/out-of-scope); enacted by the proposed D-037; **TO-015** (2026-07-15) — Node-RED enabled-without-mode auth default: **fail-secure (`secure`)** vs backward-compat (`legacy-open`) → chose fail-secure (enacted by D-041), since Node-RED is a code-execution surface that must never default to unauthenticated; **TO-016** (2026-07-17) — Node-runtime pin: chose `engines.node ">=18 <21"` (client+server) + `.nvmrc=18` (matches production Dockerfile `node:18-bookworm` for dev/prod parity; range covers CI's Node 20) + advisory (NO engine-strict, so the current Node-24 box still builds with an honest off-support warning) vs pin-20 / engine-strict / do-nothing; follow-up = bump Docker+nvmrc to Node 20 together (18 EOL, range already allows) |
| `N-`   | 081 | N-010…N-021 added 2026-07-13; N-022 (deps-not-installed → **RESOLVED**, deps now present) + N-023 (chai@5 ESM → node:assert; fast-check@3.23.2) + N-024 (independent verification + anti-drift hardening) + N-025 (machine provisioning/test-runner) + N-026 (corrupt-info fail-closed) + N-027 (bcryptjs lone-surrogate DoS, fixed by D-025) + N-028 (2026-07-14 G0 ledger/domain/status + embedded-credential incident; local remediation complete; external PAT revoke/rotate USER ACTION REQUIRED / UNVERIFIED) + **N-029 (2026-07-14 store metadata `__proto__` round-trip loss / prototype-manipulation, found on independent re-run — baseline was 41/42 not "42 passing"; fixed by D-026; suite now 47 passing, stable) + **N-030 (2026-07-14 Token & Session design-validation pass — 5 latent design defects DEF-T1..T5 found and fixed in design+ledger before Task 5 code; resolved by D-027/D-028/D-029/TO-012) + **N-031 (2026-07-14 Task 5 REQ-2 increment: Token_Service issue/verify/expiry + hardening + real-crypto P-007/P-008) + N-032 (2026-07-14 VERIFIED DEFECT + root fix: FuxaAuthDb.transaction crashed on concurrent transactions on one connection — added in-process transaction queue) + **N-033 (2026-07-14 Task 5 REQ-3 increment: Refresh_Token_Store + Token_Service.refresh + P-015, real crypto — closes N-022 refresh-path gap; §02 service layer complete, API wiring is Task 13; suite 71 passing) + **N-034 (2026-07-14 VERIFIED LOOP HAZARD + fix: the save-time integrity-guard hook watched `decisions/*.md` yet its askAgent action was told to write `04-notes.md`/`00-INDEX.md` → self-retrigger loop; rewritten strictly read-only, version 2) + **N-035 (2026-07-14 Task 7: Authentication_Service implemented + verified; §01 reconciled to D-027 tokenVersion + canonical `get` lookup (DEF-A1/A2); suite 81 passing)** + **N-036 (2026-07-14 Task 8: Role_Service + Authorization_Service implemented + verified; DEF-R1 (§05 §9 Property 13 home) reconciled + DEF-R2 (VERIFIED root defect — bootstrap gate returned 403 for the seeded admin's `account.rotatePassword`, a N-013-class deadlock; §05 §4.2/§6/§8.4 + code fixed to state the gate's ALLOW half per normative P-009) fixed at root; suite 107 passing)** + **N-037 (2026-07-14 Task 9: User_Service CRUD + password policy + atomic last-admin guard implemented + verified; DEF-U1 (atomic guard is the store `deleteGuarded` method, not a service txn — fixes the N-016 TOCTOU, closes deferred Task 2.9/2.10 + P-016) + DEF-U2 (CreateOutcome was missing the `invalid`/`validation_error` variant §2.3/§8.1 require) fixed at root; suite 127 passing)** + **N-038 (2026-07-14 Task 12: admin bootstrap + `account.rotatePassword` + mandatory known-default migration + secure enrollment implemented + verified; DEF-B1 (migration re-hash is security-NECESSARY not "recommended" — a gate alone leaves the `123456` hostile-rotation takeover) + DEF-B2 (`User_Store.list()`→`readAll()` naming) fixed at root; password policy made single-source (`password-policy.js`, anti-drift); P-009 + P-010 verified; suite 142 passing)** + **N-039 (2026-07-14 Task 13 part 1/3: authorization middleware + users/roles/account routers implemented + verified over REAL HTTP incl. live-authority revocation (D-015) + bootstrap gate/rotate/tokenVersion revocation + AC-16.4 503 fail-fast; no FUXA-core edit — the D-014 SUPERSEDE `api/index.js` edit + client cutover deferred to land together; auth/session router + composition-root next; suite 151 passing)** + **N-040 (2026-07-14 Task 13 part 2/3: auth/session router signin/refresh/signout implemented + verified over REAL HTTP — §01 §4 signin incl. DV-006 parity, §02 §6.3 refresh rotation/reuse-revoke/disabled/clear-cookie, AC-3.4 signout server-side family revoke; +2 additive Token_Service helpers; composition-root + FUXA cutover still deferred to land with client; suite 160 passing)** + **N-041 (2026-07-14 Task 13 part 3a: composition-root factory `createAuthManagementModule` implemented + verified end-to-end (assemble→runBootstrap→serve whole HTTP surface; seeded-admin gated→rotate→full-admin lifecycle over real HTTP); FUXA-core `api/index.js` SUPERSEDE edit + client cutover is the remaining part 3b, coordinated; suite 164 passing)** + **N-042 (2026-07-14 cutover pre-validation — VERIFIED the running client is numeric-`groups`-based (isAdmin/checkPermission/guard/interceptor) while the module signin payload is `{token,…,roles}`; a bare `api/index.js` SUPERSEDE breaks the client ⇒ the FUXA-core cutover is hard-coupled to the client groups→roles migration (Tasks 15–17); recommend client-first per TO-013; NO code changed — planning only)** + **N-043 (2026-07-14 Task 15 pre-validation — VERIFIED client not npm-installed (`@angular/core` absent) + no test runner (no karma/jasmine/jest, no `test` script, zero `.spec.ts`) ⇒ client code is UNVERIFIABLE here; per verify-before-implement (N-006) client impl NOT started; recommend provisioning `client/` (npm install + jest/jsdom) first; NO code changed)** + **N-044 (2026-07-15 client provisioned via `npm install --legacy-peer-deps` [pre-existing eslint@22↔cli@18 peer conflict], but VERIFIED the committed FUXA client does NOT build against its own committed deps — source uses APIs removed in the pinned majors ngx-translate@18/gridster@22/ng2-charts@10/ngx-color-picker@20 (27 src errors in app.module/settings/cards-view); `npm ci` gives the same; shipped `client/dist` is stale ⇒ client rebuild/cutover BLOCKED by upstream out-of-auth-scope dep drift; my package-lock.json drift restored; recommend Option B/C — deliver auth client clients with isolated type-check, defer build/cutover to a separate platform dep-remediation)** + **N-045 (2026-07-15 Task 15.2/15.3/15.4: client HTTP clients [AuthSignInClient / UserAdminClient / RoleAdminClient] over a pure framework-free `auth-protocol` core implemented + verified headlessly — jest 22/22 exit 0, `tsc` verify 0 errors vs Angular 18; test strategy D-036, test-mechanism deviation DV-009; only 3 client devDeps added [jest/ts-jest/@types/jest]; NO FUXA-core edit; 15.1 session-plumbing reuse + the `api/index.js` SUPERSEDE cutover still deferred by N-044)** + **N-046 (2026-07-15 VERIFIED root cause of N-044: dependabot commit `0c488d5` [ancestor of HEAD] bumped 33 client deps to Angular-21/22-targeting majors [ng2-charts@10→ng21, gridster@22→ng22, ngx-translate@18 API-removed] while the app is pinned Angular 18.2.14 and source uses the old APIs [`TranslateModule.forRoot`/`NgChartsModule`]; Initial `be8d1e7` had the compatible set [ngx-translate ^14 / ng2-charts ^4 / gridster ^18 …]; remediation = TO-014/D-037, proposed pending confirmation)** + **N-047 (2026-07-15 EXECUTED the N-044 remediation D-037/TO-014: restored Initial known-good client deps + kept jest devDeps; `npm install` exit 0, `ng build` **exit 0 / 0 errors** + fresh `client/dist/index.html`, auth jest 22/22; added `app/auth-management/testing/**` to `tsconfig.app.json` exclude. **N-044 RESOLVED, web-view UNBLOCKED**, Tasks 16/17 + D-014 cutover no longer dep-blocked. Residuals: 72 npm-audit findings to re-patch selectively at Angular-18-compatible versions; Node-24-vs-18 EBADENGINE warnings; auth UI files "unused" until 16/17 wire them)** + **N-048 (2026-07-15 ROOT CAUSE of the BLANK web page: N-047 served a JIT dev build [`npm run build` = `ng build` with `defaultConfiguration:""` → `aot:false/optimization:false`], which bootstraps blank; the shipped `client/dist` is a production/AOT build [hashed chunks, no `vendor.js`]. FUXA's `Dockerfile` confirms the canonical build is `--configuration production`. Rebuilt with `ng build --configuration production` → exit 0, byte-identical to the committed `client/dist`; server now serves the production hashed chunks 200 with `<app-root>` and no JIT `vendor.js`. N-047's "renderable" sub-claim SUPERSEDED [HTTP-200 ≠ renderable]; fix is D-038; final visual render is the user's in-browser confirm)** + **N-049 (2026-07-15 property-test `numRuns` normalized to 100 by an automated Task-16.1 pass "for faster runs" — **REVERTED**: `git checkout` restored the committed 120/150/200 counts because "for faster runs" conflicts with the standing N-006 principle and no explicit user request is evidenced; retained as an incident record, Statement NO LONGER IN EFFECT; see N-050 + §3)** + **N-050 (2026-07-15 Task 16.1 Login_Page implemented + VERIFIED: pure framework-free `LoginPresenter` + thin `standalone` `@Component` (DV-010/D-039) under `auth-management/login/`; jest 3 suites/37 tests exit 0 [+15 presenter specs], `tsc` verify 0, `ng build --configuration production` exit 0 [login AOT-compiles, "unused" until 17.4 route]; AC-11.1..11.5. Also caught+remediated a 3-part working-tree drift: server-test numRuns reduction [reverted, N-049], empty `client/node_modules` [restored via `npm install`; benign `"peer":true` lock churn reverted], and the verification-rebuild `client/dist` [restored to the committed baseline — Task 16.1 is source-only; dist rebuild is 17.4])** + **N-051 (2026-07-15 browser-error investigation: user reports "many errors" opening the web; VERIFIED via a CDP probe [installed Chrome + Node global WebSocket] that the served production build is CLEAN — home `/` 32/32 requests ok + 0 console-errors/exceptions/failed/HTTP≥400, and `/editor`,`/users`,`/device`,`/view`,`/lab` all 0 errors; could NOT reproduce. No MCP was ever configured — configured the Playwright MCP in `.kiro/settings/mcp.json` for interactive repro. OPEN: needs the user's exact F12 console errors or repro steps; likely causes = cached old JIT build / different instance / interaction-triggered / runtime toasts)** + **N-052 (2026-07-15 VERIFIED FUXA-core defect via Playwright MCP — SPA deep-route direct-load/refresh 404s for ~13 client routes [alarms/messages/notifications/scripts/reports/language/logs/events/mapsLocations/flows/apikeys/userRoles/arMarkers]: `server/main.js` uses a stale HARDCODED `express.static` route list + has NO `app.get('*')` fallback; the only SPA catch-all is trapped inside `mountNodeRedIfInstalled` which isn't mounted here [/nodered 404]. Pre-existing FUXA-core, not the auth module. **RESOLVED by D-040** [top-level SPA catch-all added to main.js]; VERIFIED 13 routes 404→200 + `/alarms` renders via Playwright 0-errors + API/assets unaffected)** + **N-053 (2026-07-15 full post-fix Playwright sweep of ALL ~20 client routes + editor interaction: every route renders with 0 console errors EXCEPT `/flows`→`/nodered` 404 [Node-RED disabled, feature-gated, not a defect]; confirms D-040 resolved the "many errors")** + **N-054 (2026-07-15 runtime-config analysis: FUXA ALREADY has runtime settings via admin `POST /api/settings` [persist `_appdata/settings.js` + live-merge + `runtime.restart(true)`]; `nodeRedEnabled` default OFF. VERIFIED security footgun: `main.js` defaults `nodeRedAuthMode` to **legacy-open (NO AUTH)** on enable-without-mode while the API path forces **secure** — inconsistent. Proposed [pending OK, FUXA-core, D-041-to-be]: keep Node-RED runtime-toggled + default OFF; fix main.js to default **secure**; gate the client `/flows` entry on live `nodeRedEnabled`; verify mount-on-restart vs process-restart)** + **N-055 (2026-07-15 Task 17 design-validation: design/08 [DES-UI-USERS] validated CLEAN — no new defect; building blocks [UserAdminClient/RoleAdminClient/ModulePermissionService/UserReadGuard/SessionStore] already exist [N-045]; implement under DV-010 [pure presenter+jest] + D-039 [standalone]; route+cutover = Task 17.4/D-014/TO-013)** + **N-056 (2026-07-15 Task 17.1 DONE+verified: `user-management/` pure presenter [access gate checking/granted/denied incl. server 401/403, list, role id→name, refresh] + `standalone` component + template, no FUXA-core edit; jest 4 suites/49 tests exit 0, tsc 0, `ng build --configuration production` exit 0; source-only, dist restored. 17.2/17.3/17.4/17.5 remain)** + **N-057 (2026-07-15 Task 17.2 DONE+verified: ONE parameterized `user-form-presenter` [create req+unique / edit immutable-username + password-omit-retains-hash AC-7.3 / send-on-type AC-7.2 / metadata preserved / single-sourced validation AC-12.4 / refresh-on-success] + `standalone` `user-form.component` [FormsModule, role checkboxes, no FUXA-core edit] + 11 jest specs; jest 5 suites/60 exit 0, build exit 0; source-only. 17.3/17.4/17.5 remain)** + **N-058 (2026-07-15 Task 17.3 DONE+verified: delete logic in page presenter [remove-row AC-12.5 / last_admin keep+msg D-009 / user_not_found refresh / 403 denied / no double-delete] + `delete-user-confirm-dialog` standalone + FULL page composition wired [Create + row Edit/Delete + hosts form + dialog]; jest 5 suites/65 exit 0, build exit 0; source-only. Page UI complete; only 17.4 route+cutover + 17.5 remain)** + **N-059 (2026-07-15 Task 17.4 D-014 cutover DESIGN-VALIDATED [verified mount points + `createAuthManagementModule` router contract + design cutover section] — NOT executed: flagged REAL gaps needing a user decision before this high-blast-radius/data-affecting/FUXA-core+client step: (1) enrollmentChannel REQUIRED+unwired blocker, (2) bootstrap data-affecting/force-rotate, (3) security currently DISABLED, (4) N-042 client groups→roles migration, (5) AuthGuard dialog→route, (6) async factory + coordinated commit + dist rebuild)** + **N-060 (2026-07-16 browser-verified the production render via Playwright MCP — app bootstraps `[LOG] FUXA v1.3.4-2860`, 0 console errors, Alarms view + nav rendered, all `/api` + `/socket.io` 200; closes N-048's pending in-browser check; ALSO caught + reverted a stale-context re-derivation drift [duplicate N-048 + edited N-047 in working tree] via `git checkout`, nothing drifted committed)** + **N-061 (2026-07-16 browser-driven verification via Playwright of the routed module pages: added non-destructive `auth/login`+`auth/users` routes [partial Task 17.4, reversible, no supersede], then FOUND + root-fixed a Task-16/17 defect invisible to headless jest — ~30 i18n keys the templates/presenters use were never added to `assets/i18n/*.json` [raw keys shown]; added them to `en.json` [ngx-translate `setDefaultLang('en')` fallback → covers all 13 locales]; verified in-browser login now shows "Username/Password/Sign In", 0 console errors; `/auth/users` gate correctly denies unauthenticated. Destructive 17.4 cutover still pending N-059 decisions)** + **N-062 (2026-07-16 interactive Playwright login test: wrong-password → module posts /api/signin, gets 401, shows generic "Username or Password is incorrect!" in the alert, STAYS on page, re-enables submit [AC-11.4/11.5 verified in-browser]; the lone console "error" is just the browser's 401-resource log, not a JS exception; precise finding — legacy FUXA 401 body has NO `error` id so the client uses its `unexpected_error` fallback [correct by design; the specific `invalid_credentials` mapping engages post-cutover]. Success path + user-mgmt-with-auth need secureEnabled+creds = cutover-test scope)** + **N-063 (2026-07-16 security-enabled Playwright cutover test [secureEnabled temp on, reverted]: verified admin/123456 live via bcrypt on the DB; module login SUCCESS path WORKS — /api/signin 200 [legacy shape, no roles], SessionStore persists {token,username,fullname,roles:[]}, navigates; CORRECTED a false empty-session reading that was a server-restart artifact. TWO real issues: (a) FUXA-CORE bug home.component.ts:437 + onLogin() read `this.hmi.layout.loginonstart/loginoverlaycolor` unguarded → TypeError on secureEnabled+empty project [reported, D-003, not auto-fixed]; (b) N-042 gap live — module writes sessionStorage but not FUXA AuthService in-memory currentUser + roles:[] vs groups-based isAdmin → cutover 17.4 scope. ERR_CONNECTION_REFUSED flood = restart artifact, not a bug)** + **N-064 (2026-07-16 root-fixed the FUXA-core empty-project login crash: `home.component.ts` `this.hmi.layout.loginonstart` + `onLogin()` `loginoverlaycolor` → optional-chaining `?.` [DV-011, D-003 deviation]; verified — diagnostics 0, `ng build` exit 0, security-ON home load 0 console errors; security reverted)** + **N-065 (2026-07-16 Task 17.4 Option-2 module-login cutover IMPLEMENTED [login.component.ts delegates session establishment to FUXA `AuthService.signIn` → groups-based guard/isAdmin recognize the login; single module file, no FUXA-core/AuthGuard/server change; reversible] + build-verified [`ng build` exit 0, diagnostics 0, design validated]; **BROWSER end-to-end verification PENDING** — Playwright MCP unstable this session [Chrome launch fails/immediate-close]; honestly NOT claimed working; contained to the additive `/auth/login` route; security reverted; 17.4 stays NOT-done until browser-verified)** + **N-066 (2026-07-16 Task 17.4 Option-2 module-login cutover BROWSER e2e VERIFIED via headless-Chrome CDP [Playwright MCP down again], security ON: `/auth/login` renders i18n, admin/123456 → `sessionStorage.currentUser{groups:-1}` + token published → reload `/` → `/editor` reachable [guard accepts admin] → 0 console errors. `/auth/users` renders but "No data" — VERIFIED cause: FUXA `GET /api/users` returns a BARE ARRAY not the module `{data:[]}` envelope [expected under Option-2, server not cutover]. Option-2 LOGIN cutover = DONE/closed N-065; full User-Management data needs the Option-1 server SUPERSEDE D-014/D-042)** + **N-067 (2026-07-16 Option-1 STAGE 1 done: `InteractiveConsoleEnrollmentChannel` added to `enrollment.js` — delivers the one-time admin secret to the operator's controlled console [process.stdout, NOT fuxa.log], resolving the N-060 lockout at the root without a redeem HTTP surface [D-035 sanctioned]; enrollment-console test 3 passing + full server suite **167 passing exit 0** [baseline 164+3, no regression]; additive, no FUXA-core, no data impact. Remaining D-043 stages 2/3/4) + **N-068 (2026-07-16 Option-1 STAGE 2 done: wired `createAuthManagementModule` into FUXA-core `server/api/index.js` behind an OFF-by-default `settings.authModuleEnabled` flag — `init()` dispatches to a verbatim-extracted `runInit(authModule)`; flag-OFF is fully synchronous zero-change, flag-ON skips FUXA `usersApi`+`authApi` and mounts the module router at the usersApi position [D-014 SUPERSEDE]; VERIFIED exact overlapping routes [usersApi=/api/users,/api/roles; authApi=/api/signin,/refresh,/signout], live `TokenAdapter.secret` getter makes build-order safe, `apiApp` consumed only post-`runtime.start()`; `node --check` 0 + diagnostics 0 + suite **167 passing** + real server restart flag-OFF boots clean + `/api/version` 200. ON path [data-affecting bootstrap] deferred to stage 4; stage 3 client groups→roles next)** + **N-069 (2026-07-16 stage-3 blast-radius finding, NO client edit: VERIFIED FUXA `checkPermission` is a DUAL model [16-bit bitmask over numeric `groups` default, OR role-NAME array over `infoRoles` when `settings.userRole`] woven across gauges/controls/services + `isAdmin` bitmask gates guard/module-permission; a full groups→roles rewrite = replacing FUXA's whole client+server authz model [D-042's big/high-risk Option-1]. RECOMMENDED lower-risk root-correct alt [candidate D-044]: module `/api/signin` PROJECTS RBAC onto FUXA's existing shape — return `groups`=admin-code + `info.roles` alongside `roles`, reusing FUXA's tested permission code [same projection principle as D-007's token groups]; shrinks the client edit to the login-seam revert. Touches D-007's client-facing stance → needs approach choice before any FUXA-core client edit)** + **N-070 (2026-07-16 D-044 IMPLEMENTED+verified: `Authentication_Service.signIn` session now projects `{...,groups,info}` — `groups`=`-1` iff injected `isAdministrator(record)` [OPTIONAL dep, passthrough fallback] else own/0, `info`=`serialize({roles})` only; composition root injects authorization; suite **169 passing** [+2 projection tests], 2 shape-assert tests updated, diagnostics 0. KEY FINDING verified from source: the existing Option-2 login seam [login.component→FUXA AuthService.signIn] already consumes `data.groups`+`data.info`, so the projection makes FUXA's UNCHANGED groups-based client work end-to-end WITHOUT any groups→roles rewrite — Option B payoff. Stage 4 flip [flags on + bootstrap force-rotate + dist rebuild + browser e2e + /users routing] remains approval-gated/data-affecting)** + **N-071 (2026-07-16 stage-4 TEMP-INSTANCE dry-run [isolated userDir/DB, port 1882, flags ON] found+ROOT-FIXED a bootstrap duplicate-seed race + an apiApp-undefined crash: FUXA `runtime` fires `users.init` [table+`setDefault` seed `123456`] un-awaited, so the N-068 synchronous module build raced FUXA's `setDefault` [DuplicateKeyError + `INSERT OR REPLACE` clobber risk] and its rejection left `apiApp` undefined→`main.js:536` crash. FIX [SUPERSEDES N-068 mechanism]: `apiApp` always built synchronously; a deferred proxy [`mountDeferredAuthModule`] mounts synchronously + 503s only identity URLs until ready; module built LAZILY on `runtime.events.once('init-users-ok')` so bootstrap REMEDIATES the seeded admin [AC-17.4] not races it; build failure = 503 not crash. VERIFIED end-to-end: console secret [migration], signin 200 w/ D-044 projection groups:-1, gated→403, rotate→200, re-signin→/api/users 200 {data:[…]}; Playwright: admin login→/auth/users renders DATA + create operator1 + 0 errors, non-admin→"Unauthorized!" + 0 errors; real server flag-OFF zero-change preserved, suite 169)** + **N-072 (2026-07-16 OPEN gap surfaced by the dry-run: fresh-deploy first-login has NO client rotate-password UI — a `mustRotate` admin signs in [200] then 403s on every protected op→interceptor logs out, and grep `client/src/**` for rotate-password=0 matches. Server rotate endpoint works [N-013]; needs a follow-up module rotate page/dialog wired to `/api/account/rotate-password`, or documented ops-CLI first-rotation. Stage 4 NOT "done" while this UI gap is open)** + **N-073 (2026-07-16 D-045 forced-rotation UI IMPLEMENTED + browser-verified → CLOSES N-072: signin gains `mustRotate`; standalone `/auth/rotate-password` [presenter+9 jest, thin RotatePasswordClient, i18n]; login routes there in-app when gated. Server suite 170, client jest 6 suites/74 [parallel OOM is a memory artifact not a failure — runInBand green], `ng build` prod 0, diagnostics 0. Browser e2e on fresh temp instance: console secret→login→AUTO-ROUTE to rotate→rotate→redirect to login→re-login new pw→full admin `/auth/users`, 0 console errors. dist restored [source-only, N-050]; real server flag-OFF intact; stage-4 real FLIP remains approval-gated)** + **N-074** (2026-07-17 stage-4 real FLIP executed live [module SUPERSEDE ON, D-043/D-044/D-045], server-side curl-verified: signin 200 w/ D-044 projection + `mustRotate`, gated→403, wrong-pass→401; bootstrap force-rotated `admin`/`123456`→one-time console secret; browser-UI acceptance delegated to user) + **N-075** (2026-07-17 final checkpoint GREEN [server 170 / client 74] + reconciled tasks.md checkbox drift after verifying each) + **N-076** (2026-07-17 TO-014 enacted — client `npm audit fix` NO `--force`: PROD 15→10, cleared lodash-es/ws/qs/body-parser/engine.io-client [PROD moderate 3→0], `package.json` unchanged [lockfile-only], `ng build --configuration production` exit 0 + jest 74/74; corrected the prior summary — `uplot` needs `--force` so it stays with Group A; Group A [8× `@angular/*` XSS + `angular-gridster2` + `uplot`] deferred = the major-bump N-044 trap)** + **N-077** (2026-07-17 Playwright MCP WORKING again → browser-verified the LIVE flip on the real instance: `/auth/login` i18n render, module SUPERSEDE live [`/api/signin` 401 `invalid_credentials` structured body, distinct from legacy N-062], brute-force limiter live [429 `too_many_attempts`+`retryAfterMs`, Pillar 4], wrong-pass UX AC-11.4/11.5, `/auth/users` unauth→`Unauthorized!`; bcrypt probe [read-only] proved admin rotated to an unknown pw [neither `123456` nor `qVJ2Tfa…`] so success-path not re-guessed [already proven N-071/N-073]; ROOT-TRACED the `[ERROR] Re` = FUXA-core `auth.service.ts:77 console.error(err)` on a handled 401/429 [NOT a module defect, cosmetic, left unchanged; optional gate-to-unexpected-statuses follow-up offered])** + **N-078** (2026-07-17 user-approved admin password RESET [read record first: mustRotate:false/tokenVersion:2 → set a STRONG cost-12 hash verbatim, NOT `123456` because bootstrap re-rotates known-defaults, bump tokenVersion→3] → then FULL success-path browser acceptance on the real instance: login→`/`, D-044 projection session groups:-1 + JWT tokenVersion:3, `/auth/users` renders DATA [module SUPERSEDE `{data:[]}` envelope, closes N-066 gap], `/editor` admin-gated loads; + DV-012 console-hygiene fix verified [`__cap===[]`, only Chromium's non-suppressible 401 resource log remains]; jest 74, ng build prod 0)** + **N-079** (2026-07-17 follow-ups enacted: (#2) i18n filled the ~44 auth keys into all 12 non-en locales [added only the 39–40 MISSING per locale, preserved pre-existing FUXA translations; JSON-parse ✓ 44/44, server serves translated values, machine-assisted→native-QA flagged]; (#1) Node pin TO-016 [engines `>=18 <21` client+server + `.nvmrc=18`, advisory]; (#3) Angular upgrade DEFERRED to post-UI/UX hardening sprint with expert assessment — verified FUXA has NO SSR/hydration so ~3 SSR-only advisories [Client-Hydration + 2× HttpTransferCache] are N/A; remaining are framework XSS-sanitization = major-bump N-044 blast-radius, tracked as security debt not dropped, CSP compensating-control offered; ng build prod 0, jest 74)** + **N-080** (2026-07-16 authenticated full-route browser QA sweep of all 19 routes as admin = 0 console-errors/0 page-errors/0 4xx, no external hosts — validates the live flip + audit-fix + console-hygiene + i18n + Node-pin with no regression; + evidence-based CSP verdict: do NOT naively add CSP because FUXA uses `eval()` for user scripts [`script.service.ts`] + inline `<script>`/`onload=`/inline `<style>` in `index.html` → a real no-`unsafe-inline`/`unsafe-eval` CSP breaks the app; meaningful CSP = separate refactor sub-project) + **N-081** (2026-07-17 CROSS-MACHINE RECONCILIATION on machine `toann` at HEAD `c45ab1c`: a stale-context session RE-DERIVED D-043-Stage2/D-044/D-045 locally [N-060 class] while the other machine had ALREADY committed the canonical equivalents through the live Stage-4 flip + N-074..N-080; VERIFIED committed tree contains the exact work [`mountDeferredAuthModule`, `rotate-password/**`, D-045] → NOTHING lost, local uncommitted copies superseded on advance to `c45ab1c`; ALSO fixed a real committed drift — N-080 was committed but high-water still read N→079, now corrected; reinforced the resume rule: re-read `00-INDEX §2` + `git log`/`git status` over any context summary)**|
| `P-`   | 016 | P-013 (D-015) + P-014 (D-014/018) + P-015 (D-019) + P-016 (D-020 concurrency) added 2026-07-13 |
| `TASK-`| see tasks.md | |

## 3. Drift-incident log (append-only)

| Date | Incident | Root cause | Remediation |
|------|----------|-----------|-------------|
| 2026-07-13 | `03-tradeoffs.md` overwritten with a chat transcript (N-020) | No integrity manifest; whole-file overwrite of a ledger file went undetected | File rebuilt from cross-references; TO-003 quarantined; this manifest + GATES + steering + audit hook added |
| 2026-07-13 | Deep review found 11 design defects (N-010…N-019, N-021) present since design phase | Design/tasks generated without a defect-gate before implementation | Defect register (traceability §F) + gates (GATES.md) added; resolutions D-014…D-023 logged OPEN |
| 2026-07-13 | Two stale ledger annotations after their underlying state changed: 00-INDEX §2 still marked D-014…D-023 / DV-006…DV-008 "OPEN"; N-022 still claimed "deps absent" (N-024) | ON-EXIT protocol (§2/status refresh) not applied when the decisions were confirmed and when `npm install` was run | Corrected both to current state; recorded provenance in N-024; added an automatic save-time integrity guard |
| 2026-07-14 | G0 transfer audit found wrong-domain entries, non-monotonic/duplicate D headings, stale OPEN/G4/task summaries, and an embedded PAT in the Git remote (N-028) | Early entries predated the strict manifest; later phase transitions did not reconcile every derived status; handoff summaries were trusted without rerunning the full domain/order check | Local repair complete: remote cleaned, IDs/substance preserved while ledgers were canonicalized, derived status reconciled, Integrity Check passed, and full baseline passed 42 tests. External PAT revoke/rotate remains USER ACTION REQUIRED / UNVERIFIED |
| 2026-07-14 | Independent baseline re-run was 41 passing / 1 FAILING (not the handoff's "42 passing"): store metadata round-trip lost a `__proto__` key (P-003, seed-dependent → flaky suite) and `Object.assign`-on-parsed-JSON could reassign an object's prototype (N-029) | Reconstructing objects from JSON-parsed (untrusted) data with `[[Set]]` semantics triggers the `__proto__` accessor; no reserved-key hardening for `__proto__`; the handoff's baseline claim was not re-verified across seeds | Root fix D-026: `deserialize` strips `__proto__` at every depth + adapters compose with object-spread define-semantics; `__proto__` excluded from P-003/P-005 domain; deterministic strip tests added; suite now 47 passing, exit 0, stable across 4 runs |
| 2026-07-14 | Save-time integrity-guard hook was a self-trigger LOOP hazard (N-034): `fileEdited` on `decisions/*.md` + an `askAgent` action instructed to WRITE `04-notes.md`/`00-INDEX.md` (same pattern) → write re-fires the hook → infinite loop; also a per-turn burst | An automatic `fileEdited` hook was given write/remediation duties on the file domain it triggers on; it also contradicted the steering's "read-only, cannot loop" claim | Rewrote the hook to version 2: strictly READ-ONLY with an ABSOLUTE ANTI-LOOP RULE (never writes; STOP-and-report on failure; remediation in a separate turn). Reconciled with the steering contract |
| 2026-07-14 | `FuxaAuthDb.transaction()` crashed with "cannot start a transaction within a transaction" when two transactions overlapped on the single shared connection (N-032), surfaced by the D-030 concurrent-consume test | The atomicity design assumed `BEGIN IMMEDIATE` serializes writers — true across connections, false within one shared connection; `transaction()` had no in-process serialization | Root fix: in-process transaction queue in `FuxaAuthDb.transaction()` (each waits for the prior COMMIT/ROLLBACK before BEGIN; gate released in finally). `BEGIN IMMEDIATE` retained for cross-connection serialization. Verified by the concurrent-consume test + full suite 71 passing |
| 2026-07-14 | Pre-Task-5 design-validation of `design/02` found 5 latent design defects (DEF-T1..T5): `Identity` missing `tokenVersion`; infeasible `kid` overlap-rotation over FUXA's single secret; parallel `typ`/`type` claims; unnamed `iss`/`aud` settings; `metadata.tokenVersion` undefined + unsafe absent-value compare (legacy-token revocation bypass) (N-030) | Design text lagged its own §3 and the code interface stub; a hardening capability was specified beyond the substrate; cross-section claim/field inconsistencies were never reconciled | Fixed at the root in design + ledger BEFORE code: D-027 (tokenVersion end-to-end, default 0 + absent→0 coercion), D-028 (single `type` claim), D-029 (named iss/aud, validate-when-configured), TO-012 (kid Option A). Runtime verification owned by Task 5/8, PENDING |
| 2026-07-14 | Task 8 authorization code returned **403** for a freshly-seeded admin (`groups:-1`, `mustRotate:true`) invoking `account.rotatePassword` → designed-in bootstrap deadlock (N-013 class), contradicting the normative P-009 (§12 §9) and §05 §10.3 (DEF-R2, N-036) | `design/05` §4.2 step 2 + §6 + the §8.4 mermaid wrote only the DENY half of the bootstrap gate; the ALLOW half (self-rotation needs no RBAC grant, so it is permitted even though `account.rotatePassword ∉ ADMIN_PERMISSION_SET`) lived only in §12/§10.3 and was never encoded in §05's decision procedure; the code implemented §05's incomplete procedure literally | Root fix in design + code: §05 §4.2 step 2 now decides the mustRotate case ENTIRELY (rotate→allow, membership NOT consulted; else→403), §6 + §8.4 mermaid updated; `authorization.service.js` `isAllowed` allows `account.rotatePassword` under `mustRotate`. Verified by the DEF-R2 regression test + full suite 107 passing |
| 2026-07-14 | Task 12 design-validation: (DEF-B1, security) `design/12` §8 made the migration re-hash of a known-default `'123456'` admin "recommended" while only `mustRotate=true` was mandatory — leaving the exact hostile-rotation takeover §3.2 closes (attacker signs in with `'123456'` → gate allows only rotate → attacker rotates → seizes the sole admin); §10.3's "no usable authority" assertion is unsatisfiable without the re-hash. (DEF-B2) §3.1/§3.4 called `User_Store.list()` but the canonical store method is `readAll()` (N-038) | §8's "recommended" wording under-stated a security necessity identical to the seed's random-secret rationale; the store method was referenced by the service verb name | Root fix: §8 corrected to make the migration re-hash NECESSARY (`bootstrap.js` re-hashes to a fresh CSPRNG secret + arms the gate + delivers via the enrollment channel); §3.1 reconciled to `readAll()`. Verified by `bootstrap.test.js` (§10.3 `'123456'` no longer verifies + no usable authority) + full suite 142 passing |
| 2026-07-14 | Task 9 design-validation: (DEF-U1) `design/04` §6.5 required an atomic `BEGIN IMMEDIATE` count+delete "on the adapter's connection" but the `User_Store` interface had only separate `readAll`/`delete` and AC-16.3 forbids the service running SQL — a service-level guard would be the N-016 zero-admin TOCTOU; (DEF-U2) `CreateOutcome` (§2.2) omitted the `invalid`/`validation_error` variant that §2.3/§8.1 (AC-4.6/4.7 + omitted password) require (N-037) | §6.5's atomicity requirement had no interface method to realize it and Task-2.9's "service is the transaction site" note contradicted the layering; the create outcome set was written before the password-policy/omitted-password rejection paths were added | Root fix: (DEF-U1) added the atomic `User_Store.deleteGuarded(username, isAdministratorFn)` adapter method (D-033), reconciled `design/04` §6.1/§6.5, closing the deferred Task 2.9 guard + Task 2.10 P-016; (DEF-U2) added the `invalid` variant to §2.2 `CreateOutcome` + the create→invalid 400 row in §8.2. Verified by `user.service.test.js` (20 passing incl. P-016 @100) + full suite 127 passing |

| 2026-07-15 | An automated Task-16.1 pass, alongside correctly implementing the Login_Page, (a) reduced 9 server property-test files' `numRuns` 200/150/120→100 "for faster runs" and self-justified it in a new N-049; (b) left `client/node_modules` wiped; (c) created a duplicate `N-049` heading (ID reuse); (d) its verification `ng build` overwrote the committed `client/dist` | A sub-task pass optimized for speed against the standing N-006 "no rigor-for-speed / validate-repeatedly" principle, and did not reconcile IDs/high-water on exit; `node_modules` was wiped by an out-of-band prune | Reverted the numRuns change (`git checkout` → 120/150/200 restored) and marked the subagent's N-049 SUPERSEDED/REVERTED (retained, append-only); renumbered the Task-16.1 note to **N-050**; restored `client/dist` + `package-lock.json` to committed (Task 16.1 is source-only); reinstalled deps; caught by the ON-EXIT `git status`/high-water reconciliation. Flagged the numRuns question to the user for explicit confirmation |
| 2026-07-16 | A session resumed from a STALE context summary (believed high-water N-044) while the repo was already at commit `32d8bc1` (N-059); it re-derived the already-committed production-build fix and appended a **duplicate `N-048`** + edited the committed `N-047` in `04-notes.md` (N-060) | A context-compaction summary carried an outdated high-water; the resuming agent appended before re-reading `00-INDEX §2` / `git log` against the true on-disk state | Caught by the ON-EXIT `git status` + duplicate-heading check; `git checkout -- 04-notes.md end.md` restored the committed versions (duplicate removed, `N-047` intact); nothing drifted was committed. Recorded in N-060; reinforced the resume rule: re-read high-water + `git status` before any ledger append |
| 2026-07-17 | On machine `toann`, a session resumed from a STALE summary (believed HEAD `197e9aa`, Stage-4 "gated/not-done", high-water ~N-073) and re-derived D-043-Stage2 (`mountDeferredAuthModule`) + D-044 (signin projection) + D-045 (rotate UI) locally — while the repo was ALREADY 7 commits ahead at `c45ab1c` with all of it (and the live Stage-4 flip + N-074..N-080) committed by machine `k.nguyen.manh.toan` (N-081). Also found N-080 committed but §2 high-water still `N-`→079 | Same as the 2026-07-16 row: a context-compaction summary froze at an old commit/high-water while the OTHER machine kept advancing + pushing; the resuming agent worked before reconciling against `git log`/`git status` | No committed drift (the parallel local work was never committed — superseded on advance to `c45ab1c`; verified the committed tree already contains the exact equivalents, so nothing lost). Recorded in N-081; fixed the N-080 high-water (→ N-081); re-reinforced: on ANY resume — especially cross-machine — trust `git log -1`/`git status`/`00-INDEX §2` over the conversation summary BEFORE touching anything |
## 4. Integrity Check (run at session start and before any phase transition)

For each file in §1: confirm it exists, contains all its required markers, and contains **no**
content from another file's domain. For each ID prefix in §2: confirm the sequence is monotonic
with no gaps except explicitly `RETIRED`/`QUARANTINED` tombstones, and that no ID is reused. If
any check fails, STOP and open an incident in §3 + `04-notes.md`.

> This check is intentionally simple enough to run by reading files (no tooling required), and is
> the exact check that would have caught N-020 the moment it happened.
